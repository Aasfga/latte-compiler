int main()
{
  int x = giveInt(5);
  if(giveBool(false))
  {
    while(debugGcsePrintInt(x * 2) == 10 && giveBool(true))
    {
      x = 0;
    }
    printInt(2 * debugGcsePrintInt(x * 2));
  }
  printInt(3 * debugGcsePrintInt(x * 2));
  return 0;
}

boolean giveBool(boolean x)
{
  return x;
}

int giveInt(int x)
{
  return x; 
}