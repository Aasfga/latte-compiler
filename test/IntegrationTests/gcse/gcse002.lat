int main()
{
  int x = giveInt(5);
  if(giveBool(true))
  {
    printInt(3 * debugGcsePrintInt(x * 2));
  }
  printInt(1 * debugGcsePrintInt(x * 2));
  return 0;
}

boolean giveBool(boolean x)
{
  return x;
}

int giveInt(int x)
{
  return x; 
}