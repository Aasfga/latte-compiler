int main()
{
  if(giveFalse() || (giveTrue() && (giveFalse() || giveTrue())) || ouch())
  {
    printString("Hello, how are you?");
  }

  return 0;
}

boolean giveFalse()
{
  return false;
}

boolean ouch()
{
  printString("Ouch!");
  return true;
}

boolean giveTrue()
{
  return true;
}